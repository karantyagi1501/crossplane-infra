{{- $ctx := . -}}
{{- $hasPublic := and $ctx.Values.subnets.public.enabled (gt (len $ctx.Values.subnets.public.items) 0) -}}
{{- $hasPrivate := and $ctx.Values.subnets.private.enabled (gt (len $ctx.Values.subnets.private.items) 0) -}}
{{- if and $ctx.Values.subnets.enabled (or $hasPublic $hasPrivate) }}
{{- $wave := "" -}}
{{- $syncOpts := "" -}}
{{- if $ctx.Values.installOrder.enabled }}
  {{- $wave = $ctx.Values.installOrder.subnetWave | quote -}}
  {{- $syncOpts = "SkipDryRunOnMissingResource=true" -}}
{{- end }}

{{- if $hasPublic }}
{{- range $ctx.Values.subnets.public.items }}
---
apiVersion: ec2.aws.crossplane.io/v1beta1
kind: Subnet
metadata:
  name: {{ .name | quote }}
  {{- if $wave }}
  annotations:
    argocd.argoproj.io/sync-wave: {{ $wave }}
    argocd.argoproj.io/sync-options: {{ $syncOpts }}
  {{- end }}
spec:
  forProvider:
    region: {{ $ctx.Values.aws.region | quote }}
    availabilityZone: {{ .availabilityZone | quote }}
    cidrBlock: {{ .cidrBlock | quote }}
    mapPublicIpOnLaunch: {{ default false .mapPublicIpOnLaunch }}
    vpcIdRef:
      name: {{ $ctx.Values.vpc.name | quote }}
    {{- if .tags }}
    tags:
    {{- range .tags }}
      - key: {{ .key | quote }}
        value: {{ .value | quote }}
    {{- end }}
    {{- end }}
  providerConfigRef:
    name: aws-provider
{{- end }}
{{- end }}

{{- if $hasPrivate }}
{{- range $ctx.Values.subnets.private.items }}
---
apiVersion: ec2.aws.crossplane.io/v1beta1
kind: Subnet
metadata:
  name: {{ .name | quote }}
  {{- if $wave }}
  annotations:
    argocd.argoproj.io/sync-wave: {{ $wave }}
    argocd.argoproj.io/sync-options: {{ $syncOpts }}
  {{- end }}
spec:
  forProvider:
    region: {{ $ctx.Values.aws.region | quote }}
    availabilityZone: {{ .availabilityZone | quote }}
    cidrBlock: {{ .cidrBlock | quote }}
    mapPublicIpOnLaunch: {{ default false .mapPublicIpOnLaunch }}
    vpcIdRef:
      name: {{ $ctx.Values.vpc.name | quote }}
    {{- if .tags }}
    tags:
    {{- range .tags }}
      - key: {{ .key | quote }}
        value: {{ .value | quote }}
    {{- end }}
    {{- end }}
  providerConfigRef:
    name: aws-provider
{{- end }}
{{- end }}
{{- end }}
